import XCTest
@testable import ContractBridge

final class ContractBridgeTests: XCTestCase {
    func testContractBridgePackage() throws {
        // There is currently no initialization code for this module, so no tests are necessary.
    }
}
